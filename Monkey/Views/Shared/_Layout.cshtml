<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href='http://fonts.googleapis.com/css?family=Roboto+Condensed|Homenaje|Exo+2|VT323' rel='stylesheet' type='text/css'>
    <title>Monkey</title>
    @Styles.Render("~/bundles/site/css")
    @RenderSection("styles", required: false)
    
    <script type="text/javascript">
        var profile = {
            name: "@HttpContext.Current.User.Identity.Name"
        }
    </script>
</head>
<body>

    @Html.Partial("_LoginPartial")

    <div class="container body-content">
        <div id="splash" class="window">
            <div class="loading">Loading<span>...</span></div>
        </div>

        @RenderBody()
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/site/js")
    <script src="/signalr/hubs"></script>
    @RenderSection("scripts", required: false)
</body>
</html>
